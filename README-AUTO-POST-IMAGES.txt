Auto post image fix installed.

This makes posts without image references use a real image from the matching WordPress uploads year/month folder instead of the logo.

Next:
cd ~/Documents/GitHub/david-travel-next
git add .
git commit -m "Use real upload images for posts without featured images"
git push

Then redeploy in Hostinger.
