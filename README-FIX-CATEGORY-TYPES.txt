Category type fix installed.

This fixes getPublishedCategories so it returns:
- name
- slug
- count

Next:
cd ~/Documents/GitHub/david-travel-next
git add .
git commit -m "Fix category type exports"
git push

Then redeploy in Hostinger.
